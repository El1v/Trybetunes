import { Box, styled } from '@mui/material';

const CustomNotFound = styled(Box)`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  padding: 1rem;
`;

export default CustomNotFound;
