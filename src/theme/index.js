import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#94b4fc',
      main: '#4c54fb',
      dark: '#3c4ca9',
    },
    background: {
      default: '#FFFFFF',
    },
  },
});

export default theme;
